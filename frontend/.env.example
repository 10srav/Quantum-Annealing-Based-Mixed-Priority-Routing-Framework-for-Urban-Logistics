# Frontend API Configuration
# Copy this file to .env and configure the values

# The plaintext API key (must match the key used to generate API_KEY_HASH in backend/.env)
VITE_API_KEY=test

# Backend API URL
VITE_API_URL=http://localhost:8000
